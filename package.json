{
  "name": "dynascope-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w dynascope\" \"npm run dev -w @dynascope/web\"",
    "build": "npm run build -w @dynascope/web && npm run build -w dynascope && cp -r packages/web/dist packages/cli/dist/static",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.7.0"
  }
}
